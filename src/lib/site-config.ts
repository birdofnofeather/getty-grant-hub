// Site-level feature flags. HAS_METHODOLOGY controls whether the /methodology
// route exists and whether links to it render. Set true when module M6 is applied.
export const HAS_METHODOLOGY = true;
